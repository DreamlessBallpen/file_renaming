""" We create a FileName class object to suit our needs when it comes to merging two filenames.
""" 

class FileNameObj:
    def __init__(self, fname, type='', delim='-'):
        self.fname = fname
        self.type = type
        self.delim = delim

        # We parse the contents into a list, with the default delimiter being a dash
        self.namelist = fname.split(delim)



